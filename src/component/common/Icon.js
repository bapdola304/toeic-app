import React from 'react';
import { Icon } from '@ui-kitten/components';

export const ArrowIosBackIcon = (style) => (
  <Icon {...style} fill = '#ffffff' name='arrow-ios-back' />
);

export const PlayIcon = (style) => (
  <Icon {...style} name='play-circle-outline' />
);

export const PauseIcon = (style) => (
  <Icon {...style} name='pause-circle-outline' />
);

export const CheckIcon = (style) => (
  <Icon {...style} name='checkmark-square-outline' />
);

export const VolumeDownIcon = (style) => (
  <Icon {...style} name='volume-down-outline' />
);

export const VolumeUpIcon = (style) => (
  <Icon {...style} name='volume-up-outline' />
);

export const RefreshIcon = (style) => (
  <Icon {...style} name='refresh-outline' />
);

